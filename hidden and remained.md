# Hidden and Remained

本文档记录项目中已经从界面隐藏、但仍保留源代码和功能逻辑的模块。隐藏模块不得在没有确认的情况下删除；重新启用时，应先验证其数据来源和产品状态。

## 隐藏记录

### 操作审计记录

- 隐藏日期：2026-08-30
- 页面：教师端 → 学生详情 → 文书助手 → 历史版本
- 源文件：`components/teacher/StudentEssays.tsx`
- 原选择器：`<div class="mx-2 border-t border-gray-100 pt-4">`
- 隐藏方式：在原容器上增加 Tailwind CSS `hidden` 类，未删除 JSX、数据读取或空状态。
- 保留功能：读取 `sharedReview.auditLog`，按时间倒序展示最近 12 条操作记录，并在没有记录时显示空状态。
- 隐藏原因：当前审计数据来自浏览器本地审阅数据，不是后端生产级审计日志。继续展示容易让用户误以为该功能已经正式启用。
- 恢复方法：删除该容器 `className` 中的 `hidden`，并验证真实后端数据、账号权限、跨设备同步及审计完整性。

## 后续记录规范

以后隐藏但不删除的模块，应继续追加：隐藏日期、页面、源文件、隐藏方式、保留功能、隐藏原因及恢复条件。
